
class UserMapper {
    static mapUserFromSqlResult(result) {
        console.log(result)
        return `new User(
            result.insertId
            result.
        )`
    }
}

export default UserMapper